import { OffsetTopDirective } from './offset-top.directive';

describe('OffsetTopDirective', () => {
  it('should create an instance', () => {
    const directive = new OffsetTopDirective();
    expect(directive).toBeTruthy();
  });
});
